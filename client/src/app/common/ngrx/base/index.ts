import { actions, types } from './base.actions';
import { reducer } from './base.reducer';
import { selectors } from './base.selectors';

export const fromBase = {
  actions,
  types,
  reducer,
  selectors
};
